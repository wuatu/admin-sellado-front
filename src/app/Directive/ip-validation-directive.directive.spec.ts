import { IpValidationDirectiveDirective } from './ip-validation-directive.directive';

describe('IpValidationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IpValidationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
