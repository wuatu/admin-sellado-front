import { RutValidationAuthDirective } from './rut-validation-auth.directive';

describe('RutValidationAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new RutValidationAuthDirective();
    expect(directive).toBeTruthy();
  });
});
