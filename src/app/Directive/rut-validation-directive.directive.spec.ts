import { RutValidationDirectiveDirective } from './rut-validation-directive.directive';

describe('RutValidationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RutValidationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
