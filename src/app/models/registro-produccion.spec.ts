import { RegistroProduccion } from './registro-produccion';

describe('RegistroProduccion', () => {
  it('should create an instance', () => {
    expect(new RegistroProduccion()).toBeTruthy();
  });
});
