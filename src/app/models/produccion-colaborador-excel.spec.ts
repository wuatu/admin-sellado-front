import { ProduccionColaboradorExcel } from './produccion-colaborador-excel';

describe('ProduccionColaboradorExcel', () => {
  it('should create an instance', () => {
    expect(new ProduccionColaboradorExcel()).toBeTruthy();
  });
});
